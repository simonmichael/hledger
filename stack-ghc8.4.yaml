# stack build plan using GHC 8.4.3

resolver: lts-12.14

packages:
- hledger-lib
- hledger
- hledger-ui
- hledger-web
- hledger-api

extra-deps:
- easytest-0.3
- cassava-megaparsec-2.0.0
- config-ini-0.2.3.0
- megaparsec-7.0.2

nix:
  pure: false
  packages: [perl gmp ncurses zlib]
