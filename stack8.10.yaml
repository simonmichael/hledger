# stack build plan using GHC 8.10.2

nix:
  pure: false
  packages: [perl gmp ncurses zlib]

resolver: nightly-2021-01-10

packages:
- hledger-lib
- hledger
- hledger-ui
- hledger-web

# extra-deps:
# - pretty-simple-4.0.0.0
# - prettyprinter-1.7.0
# - prettyprinter-ansi-terminal-1.1.2
