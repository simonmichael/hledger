# stack build plan using GHC 8.8.1

resolver: nightly-2020-01-06
# allow-newer: true

packages:
- hledger-lib
- hledger
- hledger-ui
- hledger-web

nix:
  pure: false
  packages: [perl gmp ncurses zlib]

extra-deps:
# for hledger-lib:
# for hledger:
# for hledger-ui:
# for hledger-web:
